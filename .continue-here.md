---
project: chorushive / spotify-karaoke
task: re-run calibrate_words.py --model small
status: in_progress
last_updated: 2026-03-18T04:09:16.774Z
repo: ~/work/team-brain/projects/spotify-karaoke/
---

<current_state>
word-data.json exists and is committed (18/18 songs, whisper-tiny).
demo.html is updated and committed with audio-driven word karaoke sync.
whisper-tiny hallucinated on several songs — need to re-run with --model small
for better transcription quality before the demo.
</current_state>

<completed_work>

- Diagnosed root cause: demo.html used fixed setInterval(4200ms) with no audio awareness — no sync at all
- Built sync-poc.html: side-by-side Panel A (timer) vs Panel B (word karaoke + rAF)
- Built calibrate_words.py: runs Whisper on each Deezer preview, outputs word-data.json
- Updated demo.html: replaced word sub-timer with syncTick rAF loop
  - Mode A (audio + word-data.json): audio.currentTime drives both line and word advances
  - Mode B (fallback): smooth elapsed-time progression
- Ran calibrate_words.py with whisper-tiny → 18/18 succeeded, word-data.json committed
- All 880 tests still passing
- Committed: d9c50c9 (POC + calibration tool), ead2ffe (demo.html + word-data.json)

</completed_work>

<remaining_work>

- Re-run calibration with whisper-small for better word accuracy:
    python3 calibrate_words.py --model small
  (takes ~5-15 min vs ~2 min for tiny)
  Then: git add public/word-data.json && git commit + push to deploy

- Optionally: push to Vercel so demo.chorushive.com shows real karaoke
    git push origin main

- Optionally: run npm run test:deezer to confirm all 19 Deezer previews still load

</remaining_work>

<decisions_made>

- Kept demo.html line cycling timer as safety net — syncTick skips it when audio+wordData active
- Used whisper-tiny for speed (2 min total); word timestamps are correct, transcribed text is not always right
- word-data.json is keyed by song title (e.g. "Shape of You") — must match demoSongs[].title exactly
- syncTick uses wordLines[lineIdx][wordIdx].t for highlight start, next word's .t for highlight end
- Display words (from song.lines[i].words) are kept as-is — wordLines are used only for timing, not text

</decisions_made>

<blockers>

- whisper-tiny transcription quality: Several songs (Running Up That Hill, September, Take On Me, Hotel California line 0) had 0 words in some lines or hallucinated text. The TIMESTAMPS are still valid; the word-to-timestamp mapping may be off by a word or two on those songs. whisper-small should fix this.

</blockers>

<context>
The core sync architecture is done and working. word-data.json drives demo.html's word karaoke.
The only remaining work is improving transcription quality by re-running with a better model,
then pushing to production. The demo page now has real audio-driven karaoke when the user taps.

Key files:
  public/demo.html       — the demo page (word karaoke shipped)
  public/word-data.json  — Whisper word timestamps for 18 songs (needs small model re-run)
  public/sync-poc.html   — side-by-side POC (timer vs karaoke)
  calibrate_words.py     — re-run this with --model small
  HANDOFF.md             — full session history

Test commands:
  npm test               — 880 tests, fast
  npm run test:deezer    — hits Deezer API for all 19 songs
  python3 calibrate_words.py --model small   — re-calibrate with better model
</context>

<next_action>
Start with:
  cd ~/work/team-brain/projects/spotify-karaoke
  python3 calibrate_words.py --model small

Run in background (takes 5-15 min). While it runs, optionally verify demo.html
looks right with vercel dev. After it finishes:
  git add public/word-data.json
  git commit -m "chore: recalibrate word timestamps with whisper-small"
  git push origin main
</next_action>
